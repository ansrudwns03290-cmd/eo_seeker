# EO Seeker 프로젝트 요약 (자기소개서 참고용)

## 한 줄 정의
C++ / OpenCV 기반, 실시간 단일 표적 추적 및 팬-틸트 서보 짐벌 제어 시스템. 카메라(또는 영상 파일) 입력으로부터 지정한 표적을 지속 추적하고, 표적을 놓쳤을 때 재획득하며, 최종적으로 서보모터(PCA9685, 라즈베리파이 대상)를 구동하는 임베디드 비전 파이프라인.

## 파이프라인 구조 (모듈 8개)
표적 추적을 하나의 흐름으로 잇는 8개 모듈을 직접 설계·구현함.

1. **VideoInput** — 카메라/영상 파일 입력 추상화
2. **Preprocessor** — 컬러→흑백 변환 등 전처리
3. **AcquisitionManager** — 표적 등록(ORB 디스크립터 + NCC 템플릿), LOST 상태에서 컨투어 기반 후보 탐색(morphology closing 적용), 드리프트 방지(템플릿 갱신 시 최초 원본과의 유사도 검증 후에만 허용)
4. **Tracker** — KCF 추적 + ORB/NCC 혼합 신뢰도 산출, 30프레임 주기 템플릿 갱신, 노이즈성 재시작 방지를 위한 연속 판정(streak) 로직, 재시작 직전 위치 타당성 검증
5. **StateEstimator** — 칼만 필터 기반 위치/속도 추정(predict-update), 프로세스/측정 노이즈 계수 튜닝
6. **FsmModule** — SEARCH / TRACK / LOST / REACQUIRE 4상태 유한상태기계. 신뢰도 히스토리, LOST 진입 타이머, 고속 손실 시 탐색 윈도우 확대(80→160px) 등 상황 적응형 로직
7. **ControlCommand** — 추정 위치 오차 기반 Pan/Tilt 서보 명령 생성
8. **ServoOutput** — 하드웨어 추상화. PC 빌드는 콘솔 텍스트 스텁, 라즈베리파이 빌드(`RASPBERRY_PI_BUILD` 플래그)는 PCA9685 PWM 실출력, 각도 클램핑 처리

## 부가 인프라 (직접 구축)
- **자동 로깅 시스템**: `std::streambuf`를 상속한 `TeeBuf`로 콘솔 출력을 화면과 파일에 동시 기록. 로그 파일명에 git 커밋 해시를 자동으로 남겨(`<hash>_<순번>_<영상명>.log`) "이 로그가 어떤 코드 버전의 결과인지" 항상 추적 가능하게 설계
- **디버그 이미지 덤프**: 전처리/후보 검증/최종 오버레이 프레임을 주기적으로 저장해 파이프라인 각 단계를 시각적으로 검증
- **워밍업 루틴**: 세션 시작 전 KCF FFT 플랜/ORB 캐시를 미리 예열해 1회성 초기화 비용이 세션 성능 통계를 왜곡하지 않도록 분리
- **재현 가능한 회귀 테스트**: 영상 파일 + 고정 ROI 좌표를 인자로 받아 매번 동일 조건으로 재현 테스트 가능하도록 실행 인자 규격화
- **독립 테스트 하네스 2종**: `test_kcf_standalone`(다른 모듈 없이 KCF 단독 검증), `test_servo_output`(OpenCV 의존성 없는 서보 출력 단독 검증)
- **빌드 시스템**: CMake + vcpkg(OpenCV4 contrib/ffmpeg) 의존성 관리, Windows/Raspberry Pi 두 빌드 프리셋 지원

## 개발 과정에서 해결한 주요 문제 (커밋 히스토리 기반, 2026.06.04 ~ 2026.07.19, 약 40+ 커밋)
- **오탐 방지**: 배경을 표적으로 잘못 학습하지 않도록 NCC 점수 기준 LOST 전이 로직 추가
- **신뢰도 계산 버그 수정**: 모션 블러만으로 원본 스냅샷 대비 유사도가 떨어져 정상 추적 중에도 LOST로 오판되던 문제를 실측 로그·디버그 프레임으로 원인 파악 후, "지속 갱신 템플릿" 비교 방식으로 재수정
- **드리프트 방지**: 표적 템플릿이 서서히 배경/다른 물체로 옮겨가는 것을 막기 위해, 갱신 후보를 최초 등록 원본과 항상 비교하는 anchor 검증 로직 설계
- **재획득(Reinit) 안정화**: 컨투어 측정 노이즈 한 번으로 KCF 전체를 재시작하지 않도록 연속 판정(streak) 확인 로직 추가, 칼만 예측 위치와 크게 어긋난 재시작은 보류하는 안전장치 추가
- **칼만 필터 튜닝**: 프로세스/측정 노이즈 계수를 반복 조정하며 실측값 신뢰 비율 최적화
- **제어 버그 수정**: LOST/SEARCH 전이 시 서보 명령이 (0,0)으로 튀던 문제 해결(마지막 각도 유지)
- **성능 개선**: FPS 상대시간 계산 수정, 로그 저장을 처리 경로에서 분리해 속도 개선, 카메라 대기시간 vs 처리시간을 별도 계측해 병목 진단
- **하드웨어 통합**: ServoOutput 모듈 및 PCA9685 연동, Pi/PC 빌드 분기 추가

## 사용 기술 스택
C++17, OpenCV4(KCF 트래커, ORB 특징점, 컨투어/모폴로지 연산, 칼만 필터), CMake, vcpkg, Git, (하드웨어) PCA9685 서보 드라이버 / 라즈베리파이

## 자소서에 활용하기 좋은 포인트
- 인식(비전) → 판단(FSM/칼만) → 제어(서보)로 이어지는 엔드투엔드 실시간 시스템을 단독으로 설계·구현
- 단순 알고리즘 적용이 아니라, 실측 로그·디버그 이미지를 근거로 가설을 세우고 반복 검증하며 버그를 추적한 디버깅 경험(예: 모션 블러로 인한 신뢰도 오판 사례)
- 재현 가능한 테스트 환경(커밋 해시 태깅 로그, 고정 ROI 회귀 테스트, 독립 테스트 하네스)을 스스로 구축해 품질을 관리한 경험
- PC/임베디드(라즈베리파이) 양쪽을 모두 고려한 빌드 설계 및 하드웨어 추상화 경험
